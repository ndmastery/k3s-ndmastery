apiVersion: traefik.io/v1alpha1
kind: TraefikService
metadata:
  name: web-calconversion-wrr
  namespace: ndmastery
  labels:
    app.kubernetes.io/name: web-calconversion
    app.kubernetes.io/part-of: ndmastery
    app.kubernetes.io/managed-by: argo-rollouts
spec:
  weighted:
    sticky:
      cookie:
        name: web-calconversion-route
        httpOnly: true
        secure: true
        sameSite: lax
    services:
      - name: web-calconversion-stable
        port: 3003
      - name: web-calconversion-canary
        port: 3003
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: web-calconversion
  namespace: ndmastery
  labels:
    app.kubernetes.io/name: web-calconversion
    app.kubernetes.io/part-of: ndmastery
    app.kubernetes.io/managed-by: argo-rollouts
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`calconversion.com`)
      kind: Rule
      services:
        - name: web-calconversion-wrr
          kind: TraefikService
      middlewares:
        - name: ndmastery-secure-headers
        - name: ndmastery-compress
  tls:
    secretName: web-calconversion-tls
    options:
      name: ndmastery-modern-tls
      namespace: ndmastery
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: web-calconversion-tls
  namespace: ndmastery
  labels:
    app.kubernetes.io/name: web-calconversion
    app.kubernetes.io/part-of: ndmastery
    app.kubernetes.io/managed-by: argo-rollouts
spec:
  secretName: web-calconversion-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - calconversion.com
