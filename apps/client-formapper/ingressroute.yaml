apiVersion: traefik.io/v1alpha1
kind: TraefikService
metadata:
  name: client-formapper-wrr
  namespace: ndmastery
  labels:
    app.kubernetes.io/name: client-formapper
    app.kubernetes.io/part-of: ndmastery
    app.kubernetes.io/managed-by: argo-rollouts
spec:
  weighted:
    sticky:
      cookie:
        name: client-formapper-route
        httpOnly: true
        secure: true
        sameSite: lax
    services:
      - name: client-formapper-stable
        port: 3100
      - name: client-formapper-canary
        port: 3100
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: client-formapper
  namespace: ndmastery
  labels:
    app.kubernetes.io/name: client-formapper
    app.kubernetes.io/part-of: ndmastery
    app.kubernetes.io/managed-by: argo-rollouts
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`web.formapper.com`)
      kind: Rule
      services:
        - name: client-formapper-wrr
          kind: TraefikService
      middlewares:
        - name: ndmastery-secure-headers
        - name: ndmastery-compress
  tls:
    secretName: client-formapper-tls
    options:
      name: ndmastery-modern-tls
      namespace: ndmastery
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: client-formapper-tls
  namespace: ndmastery
  labels:
    app.kubernetes.io/name: client-formapper
    app.kubernetes.io/part-of: ndmastery
    app.kubernetes.io/managed-by: argo-rollouts
spec:
  secretName: client-formapper-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - web.formapper.com
